version: "3"

services:
    minecraft-server:
        container_name: stoneblock3
        build:
            context: .
        volumes:
            - /mnt/modpack_server_data:/server
        ports:
            - 25565:25565
            - 25575:25575

volumes:
    modpack_server_data:



